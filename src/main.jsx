import React from 'react';
import ReactDOM from 'react-dom/client';
import BabuWidget from './BabuWidget.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BabuWidget />
  </React.StrictMode>
);
